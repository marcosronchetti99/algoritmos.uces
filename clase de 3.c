#include<stdio.h>
#include<stdlib.h>

int menu ();
int main (){

    int *cto1=NULL,*cto2=NULL,opc;
    cto1=malloc(sizeof(int));
    cto1[0]=0;
    cto2=malloc(sizeof(int));
    cto2[0]=0;
    opc=menu;
    while (opc!=7)
    {
        switch(opc)
    }
        case 1:do
            {
            printf ("ingrse un nro de conjunto (1,2)=")
            scanf ("%d",&conj);
            if (conj !=1 && conj!=2)
            }



    return 0;
}

int menu (){

    int resp;
    printf ("1-agregar un elemento a un conjunto\n");


}
