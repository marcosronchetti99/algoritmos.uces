char *invertir(char cadena[]) {
  int longitud = strlen(cadena);
  char temporal;
  for (int izquierda = 0, derecha = longitud - 1; izquierda < (longitud / 2);
       izquierda++, derecha--) {
    temporal = cadena[izquierda];
    cadena[izquierda] = cadena[derecha];
    cadena[derecha] = temporal;
  }
  return cadena;
}

resto = decimal%16
DECIMAL = floor(decimal/16)
hexa=resto
if hexa == 10
hexa = A
else if hexa == 11
	hexa = B
  	else if hexa = 12
		hexa = C
		else if hexa = 13
			hexa = D
			else if hexa = 14
				hexa = E
				else if hexa = 15
					hexa = F
HEXAFINAL[n]= resto
n=n+1 (para la posición del numero final)

while decimal>15

HEXAFINAL [n]=decimal

con 1200
HEXAFINAL = 0B4

har *invertir(char cadena[]) {
  int longitud = strlen(hexafinal);
  char temporal;
  for (int izquierda = 0, derecha = longitud - 1; izquierda < (longitud / 2);
       izquierda++, derecha--) {
    temporal = cadena[izquierda];
    cadena[izquierda] = cadena[derecha];
    cadena[derecha] = temporal;
  }
  return cadena;
}
        int decimal_final
        for(I=0;I=LONGITUD;I++)

            IF HEXA[i]
     if hexa == A
        decimal_final = 10*16^i
            else if hexa == B
            decimal_fina = 11*16^i
                else if hexa = C
                decimal_final 12*16^i
                    else if hexa = D
                    decimal_final 13*16^i = 13
                        else if hexa = E
                      decimal_final = *16^i = 14
                            else if hexa = F
                          decimal_final =*16^i = 15


                          else decimalfinal = hexa[i] *16^i

